name: 'sa_retail'
version: '1.0.0'
config-version: 2

profile: 'sa_retail'

model-paths: ["models"]
seed-paths:  ["seeds"]
test-paths:  ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  sa_retail:
    staging:
      +schema:       staging
      +materialized: view
    marts:
      +schema:       marts
      +materialized: table
